# Formation Data Architecture — Variables d'environnement
# Copier : cp .env.example .env
# Les valeurs par défaut (:-valeur) permettent de démarrer sans .env

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
JUPYTER_TOKEN=formation2024
AIRFLOW_DB_PASSWORD=airflow
AIRFLOW_SECRET_KEY=formation-secret-key-change-in-prod
OPENMETADATA_DB_PASSWORD=openmetadata_password
POSTGRES_DEMO_PASSWORD=retailco
POLARIS_DB_PASSWORD=polarispassword

# ── Dremio ────────────────────────────────────────────────────────────────────
DREMIO_ADMIN_USER=admin
DREMIO_ADMIN_PASSWORD=Dremio@2026

# ── Polaris OAuth2 ─────────────────────────────────────────────────────────────
# Doit correspondre à POLARIS_PRINCIPAL_ROOT_CREDENTIALS dans docker-compose.yml
POLARIS_CREDENTIAL=root:s3cr3t
