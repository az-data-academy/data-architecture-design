# ============================================================================
# Formation Data Architecture — requirements.in
# Contraintes intentionnellement souples : uv résout les conflits transitifs
# Target : Python 3.11 / 3.12 — image jupyter/pyspark-notebook:spark-3.5.0
# ============================================================================

# ── Iceberg ──────────────────────────────────────────────────────────────────
pyiceberg[pyarrow,duckdb,s3fs]>=0.11.0

# ── Nessie client (versionnage indépendant du serveur Nessie 0.105.3 Java) ─
pynessie>=0.67.0

# ── SQL / Trino ───────────────────────────────────────────────────────────────
trino>=0.330.0
sqlalchemy>=2.0.0,<3.0.0

# ── Feature Store (redis-py résolu par feast automatiquement) ─────────────────
feast[redis]>=0.40.0

# ── MLflow 2.x (nos notebooks utilisent l'API 2.x, pas 3.x) ──────────────────
mlflow>=2.20.0,<3.0.0

# ── dbt (dbt-core résolu automatiquement comme dépendance de dbt-trino) ───────
dbt-trino>=1.9.0

# ── Data Quality (API 0.18.x stable, 1.x change l'API) ──────────────────────
great-expectations>=0.18.15,<1.0.0

# ── ML / Data Science ────────────────────────────────────────────────────────
scikit-learn>=1.4.0,<2.0.0
pandas>=2.1.0,<3.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── AWS / MinIO ───────────────────────────────────────────────────────────────
boto3>=1.34.0
s3fs>=2024.0.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
requests>=2.28.0
python-dotenv>=1.0.0
